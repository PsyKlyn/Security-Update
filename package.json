{
  "name": "system-updater",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "python install.py || python3 install.py",
    "install": "python install.py || python3 install.py"
  },
  "engines": {
    "node": "*"
  }
}
